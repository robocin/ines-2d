#include <iostream>

namespace kickerStm {
inline void doMove(double ball) { std::cout << "Moving to ball pos: " << ball << "\n"; }
inline void doShoot() { std::cout << "Shooting now to goal\n"; }
} // namespace kickerStm